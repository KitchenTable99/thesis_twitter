program: quick_start_pytorch.py
method: random
name: initial_random
metric:
  goal: maximize
  name: val_acc
parameters:
  hidden_layer_size:
    distribution: log_uniform_values
    min: 32
    max: 1024
  batch_size:
    distribution: log_uniform_values
    min: 2
    max: 1024
  patience:
    min: 1
    max: 16
  lr:
    distribution: log_uniform_values
    max: 0.1
    min: 0.00001
  regularization:
    distribution: log_uniform_values
    max: 0.7
    min: 0.01
  optimizer:
    values:
      - adam
      - sdg
